// Checkbox theme configuration
// Defines styles for checkboxes in light and dark themes
import 'package:flutter/material.dart';

/// Checkbox theme configuration
class ACheckBoxTheme {
  ACheckBoxTheme._(); // Private constructor to prevent instantiation

  /// Light mode checkbox theme
  static CheckboxThemeData lightTheme = CheckboxThemeData();

  /// Dark mode checkbox theme
  static CheckboxThemeData darkTheme = CheckboxThemeData();
}
